{
  "name": "taki",
  "version": "0.2.2",
  "description": "An easy to use, fast MAL API wrapper",
  "main": "src/taki/index.js",
  "license": "MIT",
  "scripts": {
    "test": "jest",
    "test:coverage": "jest --coverage"
  },
  "devDependencies": {
    "jest": "^29.4.0"
  },
  "dependencies": {
    "dotenv": "^16.0.3"
  }
}
